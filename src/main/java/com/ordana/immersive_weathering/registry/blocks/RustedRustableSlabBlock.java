package com.ordana.immersive_weathering.registry.blocks;

public class RustedRustableSlabBlock extends WeatheredRustableSlabBlock {
    public RustedRustableSlabBlock(RustLevel rustLevel, Settings settings) {
        super(rustLevel, settings);
    }
}
