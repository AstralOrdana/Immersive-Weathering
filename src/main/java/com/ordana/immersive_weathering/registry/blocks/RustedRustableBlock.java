package com.ordana.immersive_weathering.registry.blocks;

public class RustedRustableBlock extends WeatheredRustableBlock {
    public RustedRustableBlock(RustLevel rustLevel, Settings settings) {
        super(rustLevel, settings);
    }
}
